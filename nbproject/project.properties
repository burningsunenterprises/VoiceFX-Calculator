applet.draggable=true
applet.height=200
applet.width=200
application.title=JavaFXApplication1
application.vendor=n1088802
build.classes.excludes=**/*.java,**/*.form,**/*.fx
build.classpath=\
    ${javac.classpath}:\
    ${src.dir}:\
    ${build.classes.dir}:
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
debug.classpath=\
    ${run.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/VoiceFX-Calculator.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
includes=**
jar.compress=true
javac.classpath=\
    ${libs.JavaFX-Extras.classpath}:\
    ${libs.JSAPI-SE.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javadoc.additionalparam=
javadoc.author=true
javadoc.encoding=
javadoc.private=true
javadoc.version=true
javafx.profile=desktop
jnlp.codebase.type=local
jnlp.codebase.url=file:/C:/Documents%20and%20Settings/esmith/My%20Documents/NetBeansProjects/JavaOneCode/VoiceFX/VoiceFX-2-Calculator-Spoken-Response/dist/
jnlp.offline-allowed=true
jnlp.packEnabled=false
jnlp.signed=false
main.class=com.bse.voicefx.demo.calculator_spoken_output.VoiceFXCalculator
pack200.jar.compress=true
platform.active=default_fx_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Djava.library.path=C:\\\\Progra~1\\\\Conversay\\\\JSAPI2\\\\dist\\\\ri\\\\platform
source.encoding=UTF-8
src.dir=\
    ${src.pre-src.dir}:\
    ${src.main.dir}:\
    ${src.post-src.dir}
src.main.dir=src\\main
src.post-src.dir=src\\post-src
src.pre-src.dir=src\\pre-src
